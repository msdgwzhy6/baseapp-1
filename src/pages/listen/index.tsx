import React, {FC} from 'react'
import {Text, View} from 'react-native'

interface IProps {}
const Listen: FC<IProps> = () => {
  return (
    <View>
      <Text>listen</Text>
    </View>
  )
}

export default Listen
